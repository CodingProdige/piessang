import { clientApp, clientAuth, clientDb, clientStorage } from "@/lib/firebase";

export const pricingDb = clientDb;
export const pricingApp = clientApp;
export const pricingAuth = clientAuth;
export const pricingStorage = clientStorage;
