import { piessangClientFirebase } from "@/lib/firebase/client";

// Compatibility alias while the app consolidates onto the client Firebase project.
export const bevgoPricingFirebase = piessangClientFirebase;
